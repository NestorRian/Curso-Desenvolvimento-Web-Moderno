const [a, b, c, d] = [3, 4, 1, 10];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisa0 = d / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisa0, modulo);